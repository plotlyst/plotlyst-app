\documentclass[a5paper,{{ font_size }}]{book}
\usepackage{geometry}
\usepackage{parskip}
\usepackage{setspace}
\usepackage{fancyhdr}

%\geometry{top=1in, bottom=1in, left=1in, right=1in}

\setlength{\parindent}{1em}
\setlength{\parskip}{0pt}

{% if line_spacing == "double" %}
\doublespacing
{% elif line_spacing == "onehalf" %}
\onehalfspacing
{% endif %}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}

\begin{document}

    % Title Page
    \begin{titlepage}
        \centering
        {\Huge {{ title }}}\\[1cm]
        {\Large {{ author }}}\\[3cm]
        \vfill
        \textit{ {{ subtitle }} }
        \vfill
    \end{titlepage}

    % Start page numbering after title page
    \setcounter{page}{3}

    % Include chapters from separate files
    {% for chapter in chapters %}
    \include{{ "{" ~ chapter ~ "}" }}
    {% endfor %}

\end{document}
